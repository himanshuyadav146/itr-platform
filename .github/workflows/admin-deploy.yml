# Admin Panel — CI/CD (placeholder)

> **Status:** Not configured yet. Uncomment and customize when ready to deploy.

```yaml
# name: Admin Deploy
#
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'apps/admin/**'
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#       - run: npm ci
#         working-directory: apps/admin
#       - run: npm run build
#         working-directory: apps/admin
#         env:
#           VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
#       # Deploy dist/ to hosting (FTP, S3, cPanel, etc.)
```

See [docs/ENVIRONMENTS.md](../docs/ENVIRONMENTS.md) for production config.
